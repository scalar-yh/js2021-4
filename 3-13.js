let numA = 52;
let numB = 0;
let numC = -23;

console.log(`${numA}는 ${numA > 0 ? "0보다 큰" : "0 또는 0 보다 작은"}숫자입니다.`);
console.log(`${numB}는 ${numB > 0 ? "0보다 큰" : "0 또는 0 보다 작은"}숫자입니다.`);
console.log(`${numC}는 ${numC > 0 ? "0보다 큰" : "0 또는 0 보다 작은"}숫자입니다.`);